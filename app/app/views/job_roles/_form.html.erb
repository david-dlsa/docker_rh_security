<%= form_with(model: job_role, local: true) do |form| %>
  <% if job_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_role.errors.count, "error") %> prohibited this job_role from being saved:</h2>

      <ul>
      <% job_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :job_role_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
